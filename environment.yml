name: xcube-gen
channels:
  - conda-forge
  - defaults
dependencies:
  # Required
  - oauthlib
  - pip
  - click
  - flask
  - flask-cors
  - matplotlib-base
  - boto3
  - python-jose

  # Inspection
  - jupyterlab

  - pip:
      # Required
      - requests_oauthlib
      - kubernetes

  # testing
  - moto
  - flake8
  - pytest
  - pytest-cov