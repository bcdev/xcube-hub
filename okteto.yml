name: xcube-gen
image: quay.io/bcdev/xcubehub-base:latest
forward:
  - 8080:8080
  - 8000:8000
remote: 2222
sync:
  - .:/usr/src/app
command:
  - bash
namespace: default

securityContext:
  capabilities:
    add:
      - SYS_PTRACE
      - SYS_ADMIN